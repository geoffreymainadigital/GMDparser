# Tests Suite (`tests/`)

This directory houses automated test suites and test fixtures across all phases:
- Functional transaction payload tests.
- Rejection and edge case tests (duplicates, malformed amounts, missing codes).
- Formula and spreadsheet boundary preservation tests.
- End-to-end network tests (Android → Vercel → Apps Script).
- SMS parser fixtures and edge cases.
